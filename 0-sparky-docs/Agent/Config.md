Should be extremely minimal. Basically only enough info to know where the manager lives, and should be fed in as a command line argument.

`sparky agent --manager-host "https://<ib-ip>:<manager-port>/"`